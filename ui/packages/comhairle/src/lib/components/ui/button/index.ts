import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";
import LoadingButton from "./loading-button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	LoadingButton,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
