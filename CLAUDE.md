# CLAUDE.md

Claude Code reads this file automatically. To avoid a second copy of the rules that
drifts out of sync, it just imports the shared agent guide. Everything lives in
[AGENTS.md](AGENTS.md) (and the docs it points to: `STYLE_GUIDE.md`, `CONTEXT.md`,
`documentation/adr`).

@AGENTS.md

<!--
Claude-only notes can go below the import if we ever need them. If you find agents
skipping the frontend conventions, add `@STYLE_GUIDE.md` here to force it into context.
-->
